$('.btn').on('click', function() {
    $(this).toggleClass('active');
    $('#Header').toggleClass('active');
    $('.globalnavi-inline').toggleClass('active');
    $('html').toggleClass('active');
});

